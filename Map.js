function getParent(node) {
  var parent = node.
}

function reformTree(node) {

}

function Map() {
  Map.prototype.Entry = function() {
    var key;
    var value;
  }
  var array = [];
  function get(var key) {
    for(array)
  }

  Map.prototype.put = function(key, value) {
    array.put
  }
}
